# Grafana 데이터소스 자동 프로비저닝 — Loki 연결 설정
# Grafana 시작 시 자동으로 Loki 데이터소스를 등록한다.

apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
